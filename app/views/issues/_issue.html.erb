<div class="card mb-3", id="issue-<%=issue.id %>">
<div class = "card-body">
  <div class="card-title d-flex w-100 justify-content-between">
  <h5 class=\"text-dark mb-1\"><%=issue.title%></h5>
  <small class = "text-primary"><%= link_to "Case No. #{issue.id}", "#" %></small>
  </div>
  <div class="card-text mb-1 text-dark text-secondary"><%=issue.description.truncate(150)%></div>
  <small class = "text-muted">Reported by <%= link_to issue.customer.name, "#" %> <%= time_ago_in_words(issue.created_at)%> ago.</small>
</div>
</div>